<article>
  <h3>{{pizza.name}}</h3>
  <div class="price">
    {{pizza.price}}
  </div>
  <div class="description">
    {{pizza.description}}
  </div>
  <button {{action 'addToOrder'}}>Add</button>
</article>